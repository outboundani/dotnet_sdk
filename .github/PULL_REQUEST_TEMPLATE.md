## What this changes

<!-- A sentence or two. Link the issue if there is one. -->

## Checklist

- [ ] Tests added or updated (`EndpointSpecTests` if this touches the wire format)
- [ ] `CHANGELOG.md` updated under `## [Unreleased]`
- [ ] README updated, if this changes the public API
- [ ] `dotnet format --verify-no-changes` passes
- [ ] `dotnet test -c Release` passes on all target frameworks

## Parity with the TypeScript SDK

<!-- The two SDKs are meant to behave identically. If this changes retry, timeout, or error
     semantics, say whether the TypeScript SDK needs the same change, or why they should differ. -->
